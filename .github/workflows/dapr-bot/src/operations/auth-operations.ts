import { getOctokit } from "@actions/github";
import { Configuration } from "../configuration";

export class AuthorizationOperations {
    public async isAuthorized(): boolean {

    }

    private whoAmI(): string {
        if (!)
    }
}